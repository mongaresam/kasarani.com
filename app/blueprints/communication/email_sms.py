"""Email/SMS dispatch helpers (stubs)."""

def send_email(to: str, subject: str, body: str) -> bool:
    """Send an email — integrate SMTP or SendGrid here."""
    return True

def send_sms(to: str, body: str) -> bool:
    """Send an SMS — integrate Africa's Talking or similar here."""
    return True
